function stringConcat(){
    let firstName = "Karson";
    let lastName = "Cheng";

    let fullName = firstName + " " + lastName; // Using + operator
    let greeting = `Hello, ${firstName} ${lastName}!`; // Using template literals
    console.log(greeting);
}