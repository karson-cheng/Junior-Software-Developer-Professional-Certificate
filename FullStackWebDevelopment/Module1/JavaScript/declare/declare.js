function declareVariables(){
    // Exampke: Variable Declaration
    var myName = "Karson"; // Declaring a variable using 'var'
    let age = 32; // Deckaring a varible using 'let'
    const country = "Hong Kong"; // Declaring a varible using 'const'
    console.log("Name: " + myName + ", Age: " + age + ", Country: " + country)
}